/*****************************************************************/
// 包含必须的头文件

#include <iostream>
#include <vector>
#include <queue>
#include <limits.h>
#include <math.h>
#include <bits/stdc++.h>
using namespace std;

// 定义本地
#define LOCAL_ 1

// 数据 通过 100

/*****************************************************************/

const int MAX = 1000000007;

long long POW(int i)
{
    long long res= 1;
    for(;k;k>>=1)
    {

    }
}
long long Cnum(int n,int m)
{
    if(n<m)
        return 0;
    long long res=  1;

    long long Xn = 1;
    long long Xm = 1;
    for(int i=1;i<=m;i++)
    {
        Xn  
    }


}


int  main(void)
{
    
    int n=0,m=0;
    #if LOCAL_
    n = 4,m=4;
    #else
    cin>>n>>m;
    #endif

    // 可以证明 为 C_{2*n-1}^{m-1}

    long long  res = fun(n+m-1,m);

    cout<<res<<endl;


    return 0;
}